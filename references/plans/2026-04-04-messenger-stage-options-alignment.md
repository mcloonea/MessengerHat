# Plan: Messenger stage options alignment

## Goal
Restrict the Stage dropdown to the approved options shown in the live source list.

## Steps
1. Replace the hard-coded Stage options in `content.js` with only the approved values and order.
2. Preserve the existing field wiring and save behavior so only the dropdown choices change.
3. Verify the edited file parses cleanly and update the AI task tracker with the implemented fix.
